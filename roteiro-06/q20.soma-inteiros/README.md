# Soma Inteiros

Escreva a função `soma-inteiros` em Clojure que some os inteiros
de uma sequência qualquer. Use apenas recursividade simples (não
vale usar funções de alta ordem, nem `loop..recur`). Escreva no
mesmo arquivo uma sequência de testes/asserts que demonstrem a
funcionalidade.
