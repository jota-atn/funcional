# Count

Escreva a função `count` em Clojure que calcula a quantidade de
elementos em uma sequência qualquer. Use apenas recursividade
tail call (não vale usar funções de alta ordem). Escreva no
mesmo arquivo uma sequência de testes/asserts que demonstrem a
funcionalidade.
