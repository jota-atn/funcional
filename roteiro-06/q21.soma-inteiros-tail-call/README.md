# Soma Inteiros

Escreva a função `soma-inteiros` em Clojure que some os inteiros
de uma sequência qualquer. Use apenas recursividade tail call
(não vale usar funções de alta ordem). Escreva no mesmo arquivo
uma sequência de testes/asserts que demonstrem a funcionalidade.
